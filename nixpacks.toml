[phases.setup]
nixPkgs = ["python310", "nodejs-18_x"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "cd spendsense/ui && npm install"
]

[phases.build]
cmds = [
  "cd spendsense/ui && npm run build"
]

[start]
cmd = "uvicorn spendsense.api.main:app --host 0.0.0.0 --port $PORT"
